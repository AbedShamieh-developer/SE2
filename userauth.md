# Objective (Functional Requirments)
We want to create user authentication system:
- user has name, email, password
- user can login into the system with email and password
- user can logout if he was logged in
- all order operations must be accessedby the authenticated user only

# Non Functional Requirments
- Create user model that has a name, email, password
- Create user repository  for sqlite
- Create user controller
- Add user API design
